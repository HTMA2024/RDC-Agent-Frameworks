# 平台本地 `common/` 占位说明

当前目录是平台本地 `common/` 的最小占位目录，不是正式运行时内容。

使用方式：

1. 将仓库根目录 `analyzer/common/` 整体拷贝到该平台根目录的 `common/`，覆盖当前目录。
2. 完成覆盖后，再在对应宿主中打开该平台根目录使用。

约束：

- 平台内所有 skill、agents、config 只允许引用当前平台根目录的 `common/`。
- 未完成覆盖前，当前平台模板不可用。
